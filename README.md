# Docker image of an Apache Cassandra Node

This image is based in the project [Cassandra Base Image for Docker](https://github.com/prodriguezdefino/docker-cassandra-base). 

## Running a container with a seed node

Assuming that there are no other seed nodes in the network we can run a container as a node with the command ```d```. 
